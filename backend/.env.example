# Backend Environment Variables

# Node environment
NODE_ENV=development

# Server port
PORT=5050

# MongoDB connection string (also accepts MONGODB_URI)
# - If using auth: mongodb://username:password@host:27017/zyntherraa
# - URL-encode special characters in password (e.g. @ -> %40, # -> %23)
# - If user was created in admin db, add: ?authSource=admin
MONGO_URI=mongodb://localhost:27017/zyntherraa

# JWT secret key
JWT_SECRET=your_jwt_secret_key_here

# Upload path for files
UPLOAD_PATH=./uploads

# CORS: allowed frontend origins (comma-separated).
# In production, backend defaults to https://zyntherraa.com and https://www.zyntherraa.com if empty.
# For local dev: CORS_ORIGIN=http://localhost:3000
# For production (optional override): CORS_ORIGIN=https://zyntherraa.com,https://www.zyntherraa.com
CORS_ORIGIN=
